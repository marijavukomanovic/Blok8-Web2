export enum PassengerType {
    Student = 'Student',
    Penzioner = 'Penzioner',
    Regularan = 'Regularan'
}

export enum TicketType{
    Hourly = 0,
    Daily = 1,
    Monthly = 2,
    Annual = 3
}

export enum DayType{
    Workday = 0,
    Saturday = 1,
    Sunday = 2
}

export enum RouteType{
    Suburban = 0,
    Town = 1
}

export enum VerificationType{
    Process = 0,
    Accepted = 1,
    Rejected = 2
}